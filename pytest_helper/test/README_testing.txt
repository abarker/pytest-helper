To run the tests, execute each file in the test_dir_tree directory.  Each
should run a Pytest test, and all of them should succeed.

Future thing to maybe try: add a temporary __init__.py to each test dir
and make sure things still work (since some people do that, though not
recommended).
